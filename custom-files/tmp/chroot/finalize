#!/usr/bin/env fish

# finalize — runs INSIDE the chroot (last step).
# Reconfigures all packages so the kernel, initramfs, and any other
# post-install hooks are fully applied before first boot.

source (dirname (status filename))/die

echo "=== Finalizing ==="
run xbps-reconfigure -fa
echo ">>> finalize done."
